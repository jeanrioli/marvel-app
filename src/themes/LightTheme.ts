import { DefaultTheme } from 'styled-components';

const LightTheme: DefaultTheme = {
	colors: { primary: '#f0141e', secondary: '#eeeeee', tertiary: '#202020', quaternary: '#fff' },
};

export default LightTheme;
